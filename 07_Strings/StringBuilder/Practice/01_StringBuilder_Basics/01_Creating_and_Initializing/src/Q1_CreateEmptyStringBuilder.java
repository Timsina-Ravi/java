// 1. Create an empty `StringBuilder`.

public class Q1_CreateEmptyStringBuilder {
    public static void main(String[] args) {

        // Create an empty StringBuilder with default capacity (16 characters)
        StringBuilder stringBuilder = new StringBuilder();

        // Print the content of the StringBuilder (empty string, so nothing will be shown)
        System.out.println(stringBuilder); // Output: (empty line)

    }
}
